.mono-agent/
.mono-agent-build.json
.mono-agent-build.json.tmp-*
.mono-agent-build.lock
node_modules/
dist/
coverage/
.workflow/
.ultrawork/
.env*
!.env.example
.DS_Store
npm-debug.log*
pnpm-debug.log*
yarn-error.log*
mono-agent.config.json
packages/*/dist/
packages/*/types/
demos/*/dist/
# Local SQLite sidecars are disposable only when the owning package says so.
memory.db
*.db-wal
*.db-shm

# Superpowers working docs (specs/plans) — local-only, not tracked in git
docs/superpowers/

# Local-only agent notes + git hooks (never commit; keep personal identity/policy off the public repo)
AGENTS.local.md
.claude/settings.local.json
.githooks/
