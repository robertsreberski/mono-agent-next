interface:
  display_name: "Mono Agent Composer"
  short_description: "Build and validate a config-first mono-agent project"
  default_prompt: "Use $mono-agent-composer to build this mono-agent project from a strict config, validate it, and prove one real foreground smoke test."
