# @mono-agent/channel-openai-api

Authenticated OpenAI-compatible Chat Completions over a loopback-only listener.

## Category

<!-- package-metadata:start -->
<!-- Generated by scripts/generate-package-docs.mjs. Do not edit by hand. -->

Category: `communication`
Tier: `core`
Catalog responsibility: Serves one selected agent through a bounded authenticated OpenAI-compatible API.

<!-- package-metadata:end -->

Communication channel.

## Responsibility

Expose model discovery and streaming/non-streaming Chat Completions, normalize bounded inline images and conversation identity, and dispatch authenticated requests into Core without treating the facade model ID as a Core model override.

## Install / Usage

```bash
pnpm add @mono-agent/channel-openai-api
```

Select the module under `channels`. `apiKey` is env-only. The listener is always loopback and serves `GET /v1/models` plus `POST /v1/chat/completions` by default. `maxRunMs` bounds Core dispatch and `maxResponseBytes` bounds normalized response text and emitted response bytes.

## Architecture

### Data flow

Authenticated OpenAI request -> exact Host/Origin authority and body validation -> message/image normalization -> bounded Core dispatch -> size-bounded JSON response or SSE chunks ending in `[DONE]`.

### Package structure

| Source | Responsibility |
| --- | --- |
| `config.ts` | Strict env-only auth and safe loopback configuration. |
| `translation.ts` | OpenAI request validation and normalized channel requests. |
| `server.ts` | Authenticated HTTP lifecycle, JSON/SSE rendering, and cancellation. |
| `index.ts` | Typed channel module and health. |

## Public API

### Start here

| Export | Use |
| --- | --- |
| `monoAgentModule` | Load the channel through Core. |
| `createOpenAiApiServer` | Embed or deterministically test the transport. |
| `parseOpenAiChatRequest` | Validate and normalize one Chat Completions request. |
| `parseOpenAiApiConfig` | Validate already resolved module config. |

<!-- public-api-inventory:start -->
<!-- Generated by scripts/generate-public-api-docs.mjs. Do not edit by hand. -->

Every symbol exported by each public code entrypoint is listed below.

**`@mono-agent/channel-openai-api`**

```text
CreateOpenAiApiServerOptions
DEFAULT_OPENAI_API_BASE_PATH
DEFAULT_OPENAI_API_HOST
DEFAULT_OPENAI_API_MAX_BODY_BYTES
DEFAULT_OPENAI_API_MAX_IMAGE_BYTES
DEFAULT_OPENAI_API_MAX_RESPONSE_BYTES
DEFAULT_OPENAI_API_MAX_RUN_MS
DEFAULT_OPENAI_API_MODEL_ID
DEFAULT_OPENAI_API_PORT
OpenAiApiConfig
OpenAiApiConfigError
OpenAiApiHealth
OpenAiApiModuleChannel
OpenAiApiServer
OpenAiApiStartInfo
OpenAiChatRequest
OpenAiDispatch
OpenAiRequestError
createOpenAiApiServer
isLoopbackHost
monoAgentModule
openAiApiConfigSchema
parseOpenAiApiConfig
parseOpenAiChatRequest
toChannelRequest
```

<!-- public-api-inventory:end -->

## Dependency Boundary

Depends only on `@mono-agent/module-sdk` and Node HTTP primitives. It does not import Core, a runtime, state, products, or another channel.

## What This Package Does Not Own

It does not expose a public-network listener, terminate TLS, execute models, select Core runtimes/models, persist OpenAI client history, honor sampling knobs that Core cannot represent, or emulate every OpenAI endpoint. Divergent streamed text replacements and unsupported reply events fail the request instead of producing a misleading OpenAI response.

## Related Documentation

- [V1 architecture](../../docs/reference/v1-architecture.md)
- [V1 product requirements](../../refactor/mono-agent-v1-prd.md)

## Verification

```bash
pnpm --filter @mono-agent/channel-openai-api build
pnpm --filter @mono-agent/channel-openai-api typecheck
pnpm --filter @mono-agent/channel-openai-api test
```
