---
name: frontmatter
description: Use the YAML frontmatter description as the canonical skill summary.
---

# Frontmatter Skill

Body prose the index falls back to only when frontmatter has no description.

## Usage

Keep one skill folder loadable everywhere.
