# @mono-agent/channel-telegram

Authorized Telegram Bot API polling and exact-destination delivery for mono-agent.

## Category

<!-- package-metadata:start -->
<!-- Generated by scripts/generate-package-docs.mjs. Do not edit by hand. -->

Category: `communication`
Tier: `core`
Catalog responsibility: Maps Telegram Bot API updates and deliveries onto normalized channel turns.

<!-- package-metadata:end -->

Communication channel.

## Responsibility

Consume one bot update stream, enforce exact chat authorization, normalize text
and bounded media, dispatch turns, project supported live-input, cancellation,
and AskUser controls, and deliver idempotent proactive messages and files.

## Install / Usage

```bash
pnpm add @mono-agent/channel-telegram
```

Select `@mono-agent/channel-telegram` under `channels`. `botToken` must be an
environment reference; inline secrets are rejected by Core. Configure
`allowedChatIds` explicitly unless the intentionally broad `allowAllChats`
switch is true:

```json
{
  "$use": "@mono-agent/channel-telegram",
  "botToken": { "$env": "TELEGRAM_BOT_TOKEN" },
  "allowedChatIds": ["123456789"],
  "defaultDestination": "123456789"
}
```

## Architecture

### Data flow

Telegram polling -> exact chat allowlist -> bounded attachment download ->
normalized `ChannelInboundRequest` -> Core -> final reply. Supported Core
controls route `/cancel`, live steering, and bounded AskUser button/free-text
answers. Core proactive delivery -> exact Telegram destination -> in-process
idempotent Bot API send.

### Package structure

| Source | Responsibility |
| --- | --- |
| `config.ts` | Strict configuration and env-only secret schema. |
| `bot.ts` | Injectable Bot API transport and bounded HTTP implementation. |
| `delivery.ts` | Exact-destination idempotent proactive delivery. |
| `index.ts` | Poll lifecycle, normalization, module definition, and health. |

## Public API

### Start here

| Export | Use |
| --- | --- |
| `monoAgentModule` | Load the selected channel through Core. |
| `createTelegramChannel` | Inject a deterministic transport or embed the channel. |
| `createTelegramBotApiClient` | Use the bounded first-party Bot API client. |
| `parseTelegramConfig` | Validate already resolved package configuration. |

<!-- public-api-inventory:start -->
<!-- Generated by scripts/generate-public-api-docs.mjs. Do not edit by hand. -->

Every symbol exported by each public code entrypoint is listed below.

**`@mono-agent/channel-telegram`**

```text
CreateTelegramChannelOptions
DEFAULT_TELEGRAM_MAX_ATTACHMENT_BYTES
DEFAULT_TELEGRAM_POLL_SECONDS
MAX_TELEGRAM_ATTACHMENT_BYTES
TelegramBotClient
TelegramBotClientFactory
TelegramCallbackUpdate
TelegramChannel
TelegramConfig
TelegramConfigError
TelegramDelivery
TelegramMessageUpdate
TelegramReactionConfig
TelegramRemoteAttachment
TelegramSendAttachmentRequest
TelegramSendMessageRequest
TelegramUpdate
createTelegramBotApiClient
createTelegramChannel
monoAgentModule
parseTelegramConfig
telegramConfigSchema
```

<!-- public-api-inventory:end -->

## Dependency Boundary

Depends only on `@mono-agent/module-sdk` and Node/platform HTTP primitives. It does not import Core, runtimes, products, state, or another channel.

## What This Package Does Not Own

It does not persist transcripts or delivery receipts, select runtimes, create
Telegram credentials, or promise delivery after Telegram returns an ambiguous
transport failure. Successful-delivery deduplication is process-local.

## Related Documentation

- [V1 architecture](../../docs/reference/v1-architecture.md)
- [V1 product requirements](../../refactor/mono-agent-v1-prd.md)

## Verification

```bash
pnpm --filter @mono-agent/channel-telegram build
pnpm --filter @mono-agent/channel-telegram typecheck
pnpm --filter @mono-agent/channel-telegram test
```
